export class UserLogin {
    constructor(
        public id?: any,
        public typeKey?: string,
        public stateKey?: string,
        public login?: string,
        public removed?: boolean
    ) {
    }
}